# 022 — V4: Voronoi cell-graph planets (structure-first worldgen)

## Status

- State: In progress — **M0-M4e all implemented and verified** (graph core, tectonics, climate/biomes/rivers/coastlines, debug lab, canonical elevation function, chunking, per-chunk discrete LOD, local collider patches with Jolt physics wiring, and the terrain-edit rebuild path), isolated POC, not integrated with existing terrain/CDLOD; `/cell-planet-lab` now has all planned 2D map modes (graph/plates/elevation/land/temperature/moisture/biome/feature/rivers) plus a chunked, 2-level-LOD 3D preview mesh (one draw call per ~100-cell chunk, each showing full per-cell detail or sag-bounded merged cell polygons depending on camera distance), a debug high-res collider-patch overlay tracking the camera, a flatten/dig terrain-edit tool that rebuilds only the handful of chunks an edit actually touches, a `worldSizeTier` picker that genuinely rescales the render (not just a label), and (2026-08-29, ahead of M5) a `worldProfile` picker with per-cell geological features and lava rendering — see "World profiles + per-cell terrain features" below. **M4 reshaped 2026-08-26** from a single "rendering spike" into sub-milestones M4a–M4e (see Layer 2 and Milestones below), all of which are now done — **M5 (write-up & decision) is the next remaining piece.** M4d's actual Jolt ball-drop physics test now lives in its own sibling lab, `/planet-physics-lab` (2026-08-29 follow-up, see M4e's entry below), positioned in real meters via Jolt's double-precision `RVec3`.
- Date: 2026-08-28
- Ahead-of-M5 addition (2026-09-12): deterministic mountain ridge paths and summit markers are now
  derived from tectonic collision boundaries plus connected high cells and are visible in the map
  and reusable globe renderer; see "Mountain ridge network pass" below.
- Naming note: "V4" is Bruno's label (V1–V3 = the noise-first planet attempts in BSP). Sublibrary name `worldgen` below is a **placeholder, not approved**.

## TL;DR

Planet generation flips from noise-first (V1–V3: hard to control, mushy coastlines, no guaranteed flat land) to structure-first: a spherical Voronoi **cell graph** where continents, mountain ridges, biomes, and rivers are decided as discrete per-cell/per-edge properties, and noise is demoted to small surface detail. Rendering is deliberately an open question — CDLOD is *not* assumed; a cell-native adaptive mesh is the leading candidate because the authored structure lives on the graph, not in a heightfield. Everything is built and tested in isolation (pure sublibrary + dedicated lab page) before any integration.

## Why (V1–V3 post-mortem, condensed)

Four iterations of global 3D noise (fractal baseline → signed continents → domain warping + biome masks → calibrated masks + live sliders) all fought the same root cause: with noise, every gameplay requirement is an *emergent hope*, not a guarantee. Specifically:

- Mountain ridges never form connected walls that block ground movement.
- Coastlines are isolines through a continuous field — never crisp.
- "Flat buildable meadows" require masking hacks that then flatten the mountains too.
- Control knobs are noise frequencies/amplitudes, which don't map to gameplay intent.

## Goals

Unique, fun, controllable planets for physics gameplay (flying/driving vessels, base building):

1. **Mountain ridges as movement blockers** — connected ridge cells, guaranteed by construction (plate boundaries), not by noise luck.
2. **Clear continents & crisp shorelines** — land/water is a per-cell boolean; the coastline is a polyline of cell edges.
3. **Discrete biomes** — climate-driven (desert, tundra/ice caps, jungle/rainforest, taiga, savanna) plus relief-driven (meadow, hills, canyon, mesa, alpine, dunes) as *cell tags* with enforced properties (meadow ⇒ flat ⇒ buildable).
4. **Rivers that always reach the sea** — downhill walks along cell edges.
5. **Planetary scale, shape-agnostic** — sphere first; the same graph algorithms run on a flat/infinite 2D lattice later.
6. **Meaningful knobs** — "plate count", "ridge height", "sea level %", "cell density" instead of octaves and lacunarity.
7. **Deterministic & seed-driven** — same seed ⇒ same planet, pure functions, unit-testable without Three.js.

Deliberately irregular cells (relaxed Voronoi, per Amit Patel's *Polygonal Map Generation*), **not** a hex grid — irregularity hides the tiling, avoids the icosahedron's 12 pentagons, and gives more variety.

## Architecture

### Layer 1 — Cell graph (pure, framework-free)

```
(seed, params) → PlanetGraph {
  cells,       // spherical Voronoi cells: center, polygon corners, neighbor ids
  edges,       // shared boundaries with left/right cell ids
  plates,      // plate id per cell, plate motion vectors
  elevation,   // per-cell (+ per-corner interpolated) elevation
  water,       // per-cell boolean + lake detection
  biomes,      // per-cell biome tag
  rivers,      // edge chains from source cells to coast
  coastlines,  // extracted closed polylines of land/water edges
}
```

Pipeline stages (each pure, independently testable):

1. **Point distribution** — Fibonacci spiral on the sphere → N seed points (params: cell count, optionally density variation later).
2. **Lloyd relaxation** — k iterations of move-to-centroid + reproject, to turn clumpy into nicely irregular.
3. **Spherical Voronoi** — 3D convex hull of the points *is* the Delaunay triangulation; Voronoi cells are its dual (corners = triangle circumcenters projected to sphere). No spherical-geometry special cases.
4. **Plates** — seed P plates, flood-fill cells, assign each plate a random motion vector. Edge classification: convergent / divergent / transform from relative motion.
5. **Land/water** — plate type (continental/oceanic) + a low-frequency perturbation ⇒ per-cell boolean, targeting a sea-level % param. Small islands/lakes emerge from the perturbation.
6. **Elevation** — combined from: convergent boundary uplift (ridge cells, decaying with graph distance from the boundary), BFS distance-from-coast for continent interior swell, and per-biome amplitude. Elevation is authored per cell, with corner values blended from adjacent cells so slopes are controllable.
7. **Climate** — two derived per-cell scalars, computed before biome rules:
   - **Temperature** — from latitude (pole-to-equator falloff) minus elevation lapse (higher = colder). Poles get ice caps *by construction*, not by noise luck.
   - **Moisture** — BFS distance-from-ocean/river (closer = wetter), optionally biased by a simple prevailing-wind direction per plate/latitude band (windward coasts wetter, leeward interiors drier — enough to justify deserts forming in continental interiors/rain-shadows without a full atmospheric sim).
8. **Biomes** — a Whittaker-style lookup over (temperature, moisture, elevation, slope), Red-Blob-Games style:

   | | dry | medium | wet |
   |---|---|---|---|
   | **cold** (poles/high alpine) | tundra | taiga/snow | glacier/ice cap |
   | **temperate** | steppe/dunes | meadow / hills | jungle/temperate forest |
   | **hot** (equatorial) | desert | savanna | rainforest/jungle |

   Plus elevation/slope overrides independent of climate: **alpine** (high elevation, any climate), **canyon** (high slope + low moisture history), **mesa** (stepped elevation + dry). Each biome carries its *detail recipe* (noise amplitude/character used only within that cell, e.g. meadow ≈ 0, dunes = rolling low-frequency, canyon = carved steps). Ice-cap cells at the poles are also flagged as water-equivalent for rendering (frozen ocean) vs. land ice (glacier) so shorelines stay meaningful near poles.
9. **Rivers** — pick high-moisture/high-elevation source cells, walk strictly downhill along cell corners/edges to the sea; widen by accumulated flow; carve elevation along the path; contribute back into the moisture pass (river-adjacent cells get a moisture bonus, which is what lets jungle hug a river through an otherwise drier belt).
10. **Coastline extraction** — walk land/water edges into closed polylines (needed for crisp-shore rendering and shoreline gameplay queries).

### Layer 2 — Rendering (reshaped 2026-08-26 — chunked cell-native mesh, NOT CDLOD)

CDLOD (runbook 019) was painful, is in an unfinished state, and was designed for continuous heightfields. V4's source data is a polygonal graph, which suits a different architecture. Three lessons from the V1–V3 and CDLOD attempts drive this shape directly:

- **Draw calls were the actual killer before**, not triangle count — so the unit of rendering is a **chunk** (a merged run of ~100–ish cells baked into one `BufferGeometry`), never one draw call per cell.
- **Camera and physics are decoupled** — you can orbit to see the whole planet while a vessel sits on the surface, so the visual mesh's LOD and the collider's resolution must be free to differ completely. They should never be forced into "one mesh serves both."
- **Terrain is going to be edited at runtime** (base-building flatten/dig), so the height source can't be a one-shot bake — it has to be a live function that both edits and rebuilds can call at any time.

The bridge across all three is a single **canonical elevation function**, `sampleElevation(pointOnSphere) → height`: pure, deterministic, derived from the cell graph (barycentric blend of the 3 corner elevations of whichever cell the point falls in), plus — once base building lands — a sparse **override layer** (player edits stored as save data, checked before the graph value). Everything else reads from this one function instead of agreeing with each other by convention:

- **Visual chunks** displace their vertices by sampling it at whatever density their current LOD level calls for.
- **Colliders** are small, high-resolution mesh patches sampled from the same function, generated only near vessels/the player — independent of what the camera or visual LOD is doing.
- **Terrain edits** (flatten a pad) write into the override layer, then trigger a rebuild of only the affected chunk(s) and any nearby collider patches — nothing else on the planet touches disk or GPU.

LOD is **per-chunk, discrete** (2–4 fixed subdivision levels picked by camera distance to the chunk), not per-cell adaptive scoring. Border stitching only has to agree between whichever two discrete levels are adjacent, not an arbitrary continuum — far simpler than full crack-free adaptive stitching. Per-cell adaptive LOD (view-angle/horizon-weighted scoring, biome-importance budgets) is **deferred**, not abandoned: the chunk architecture doesn't block adding it later if large/medium planets or close orbital flythroughs need it, but it is not built until a real case proves it's needed. Coastline and ridge cell edges stay real mesh geometry either way (crisp by construction — no shader/depth-fade shoreline), because chunks are still built by tessellating the graph, not by rasterizing it into a heightfield.

Water rendering: separate ocean sphere mesh clipped to ocean cells, meeting the coastline polyline exactly; optional signed-distance-to-coast attribute for shore foam/shallows shading.

Because planet size is a player-facing choice (small/medium/large), the architecture must hold up across the whole range even though the near-term focus is small planets — chunking + discrete per-chunk LOD is what makes that scale, not a small-planet-only shortcut.

Exit criteria (rescoped 2026-08-26): not a single fps floor. Measure **frame time in ms for this subsystem alone** (chunk render + any active collider rebuilds) on a rotating small planet, at ground level, low grazing angle, and orbit — crisp coastline, one sharp ridge range, flat meadows, no cracks between chunk LOD levels, no visible seam between a chunk's visual mesh and its collider patch. The ms budget should leave clear headroom for everything else the game will run in the same frame (physics, other rendering, gameplay logic) — a global "60fps" number was never the real target, just a conservative sanity floor.

### Layer 3 — Physics & gameplay hooks (later, but shapes decisions now)

- Ground colliders (Jolt static mesh, streamed by proximity to vessels) are sampled from the *same* `sampleElevation()` function as the visual chunks, not literally the same mesh — see Layer 2. This is what lets the camera orbit out to see the whole planet while a vessel's collider stays high-resolution locally.
- Gameplay queries answered from the graph, not the mesh: `biomeAt(dir)`, `isBuildable(cell)`, `distanceToCoast(cell)`, `ridgeBetween(cellA, cellB)`.

## Isolation & placement

- Lives as a new pure sublibrary (working name `triangular-engine/worldgen` — **name TBD**) with its own `public-api.ts`, mirroring `scatter`/`terrain`/`procedural` conventions: deterministic core, no Angular/Three coupling, adapters at the edge.
- **No integration** with `triangular-engine/terrain` (CDLOD), `celestial` surfaces, or BSP until the POC proves out. No changes to existing planet code.
- Dedicated demo-app lab page (e.g. `/cell-planet-lab`) with live regeneration: seed, cell count, plate count, sea level, ridge height, relaxation iterations.
- 2D debug views matter as much as 3D: flat map projections of plates / elevation / biomes / rivers (Amit-style) are the fastest way to judge generation quality without touching rendering.

## Milestones

- **M0 — Graph core — done**: `triangular-engine/worldgen` sublibrary (`projects/triangular-engine/worldgen/core/`). Fibonacci points (with seeded jitter), Lloyd relaxation, convex-hull spherical Voronoi (the hull's outward face normals *are* the dual's Voronoi vertices — see the module docs on `dual-cells.ts`), neighbor graph. 18/18 unit tests passing: cell count, ≥3 neighbors per cell, symmetric adjacency, dual/Euler consistency (`Σdegree == 2·(3N−6)`), area-variance bounds, seed determinism. Run via `npm run test:triangular-engine:worldgen`.
  - Early lab page `/cell-planet-lab` (demo-app) visualizes the M0 graph with real per-cell camera-facing culling (dot-product of cell center dir vs. camera dir each tick, dynamic `BufferAttribute` + `setDrawRange`, no occluder mesh). **Known limitation**: the cull threshold is a flat dot-product cutoff, so it's only correct for a camera far from the surface (near-orthographic horizon). Up close — camera near/inside the cell radius — the true horizon is much tighter than a fixed-angle cutoff accounts for, so cells well behind the actual horizon still pass the test and get drawn. Needs a proper horizon test derived from camera altitude (e.g. threshold = f(camera distance from planet center, planet radius), or an actual horizon-plane/tangent-line check) before this generalizes past "orbit view." Ties into the M4 rendering spike's horizon/view-angle LOD weighting (line above), but that's mesh LOD, not this visibility cull — worth revisiting both together.
- **M1 — Tectonics & continents — done**: `plate-tectonics.ts` (randomized multi-source flood-fill plate assignment + per-plate type/tangent-movement), `plate-boundaries.ts` (convergent/divergent/transform classification from relative plate motion vs. boundary normal), `elevation.ts` (plate-type base elevation + per-boundary-type shaping that decays outward over a few graph hops + percentile-based sea level), combined in `tectonics.ts` (`buildPlanetTectonics()`). 14/14 unit tests passing: land fraction within tolerance of `targetLandFraction`, ridge cells (continent-continent convergent boundaries) never isolated (each has ≥1 ridge neighbor), continental mean elevation > oceanic, full determinism per seed, distinct seeds diverge. Wired into the `/cell-planet-lab` page's 2D unwrap view as three color modes (plates/elevation/land-water), plus a `plates` slider, as an early visual sanity check — full 3D preview and biome/river modes are still M3.
- **M2 — Climate, biomes & rivers — done**: `climate.ts` (latitude-driven temperature with an elevation lapse + BFS-from-ocean moisture), `biomes.ts` (Whittaker-style temperature/moisture lookup plus alpine/canyon relief overrides), `rivers.ts` (downhill cell-walk from high-moisture/high-elevation sources to ocean or a local-minimum lake), `coastlines.ts` (land/water edges chained into closed polylines via corner-triple canonicalization — see the module doc comment for why every coastline vertex has exactly 0 or 2 incident edges), combined in `ecology.ts` (`buildPlanetEcology()`). Elevation/slope/source-elevation cutoffs are all normalized as *fractions of land relief* (seaLevel..max land elevation), not absolute values, since tectonics elevation is an arbitrary unitless scale whose range varies with plate/boundary params — an early attempt at fixed absolute thresholds produced zero meadow and zero desert cells for some seeds until this was fixed. 14/14 new unit tests passing (46/46 total with M0+M1): poles are always a cold biome (tundra/taiga/glacier/ice_cap) at any seed; meadow cells have zero macro relief (slope < 8% of land relief); every river strictly descends in elevation and terminates at ocean or a lake; coastlines are closed loops (geometric closure check on the wraparound edge). Wired into the `/cell-planet-lab` page's 2D unwrap view as `temperature`/`moisture`/`biome` color modes, alongside M1's — river paths and coastline polylines are not drawn yet (that's the rest of M3's map-mode set).
- **M3 — Debug lab — done**: added a `rivers` 2D unwrap mode (land/water base + traced river paths as blue polylines + coastline loops as white polylines, both walking corner points with the same ±180°-seam segment guard as the existing edge overlay) and a naive 3D preview mesh — one flat-shaded triangle fan per cell (center → corner k → corner k+1), non-indexed geometry, `MeshStandardMaterial({ vertexColors: true, flatShading: true, side: DoubleSide })` so winding direction doesn't matter for visibility, colored via a new `resolveCellColor()` helper shared with the 2D fill so both views always agree and follow whichever map mode is selected. Geometry rebuilds on `regenerate()`; only vertex colors re-touch on a mode switch. No elevation displacement or crack-free stitching — deliberately ugly, that's the M4 rendering spike's job.
- **Rivers reworked to follow cell edges, not cell centers**: the initial `traceRivers` walked cell-center to cell-center by steepest elevation descent, with no notion of a channel direction — that produced arbitrary straight chords across a landmass (looked like a canal) and rivers that cut through a coastal cell instead of stopping at the coast. Fixed by adding `corner-graph.ts` (`buildCornerGraph()`): a dual graph over the cell graph's Voronoi corners (same corner-triple canonicalization `coastlines.ts` uses), with corner-to-corner adjacency along shared cell-polygon edges. `traceRivers` now walks this corner graph — corner elevation/moisture are the average of the 3 cells meeting there — terminating at a corner that touches water (an exact coastline vertex, a natural river mouth) or a land-locked local minimum (a lake). `IPlanetRivers.riverPaths` is now `IVec3[][]` (point chains, ready to draw) instead of cell-id chains, and `lakeCellIds` is now `lakeCorners: IVec3[]`. Rivers/ecology specs updated to verify elevation-descent and coast/lake termination against corner data instead of cell data. 46/46 tests still pass (no new tests added — same guarantees, re-expressed against corner points).
- **M2.5 — Real lake water bodies + river flow accumulation — done (2026-08-26)**: prompted by comparing this system against Amit Patel's [Polygonal Map Generation for Games](http://www-cs-students.stanford.edu/~amitp/game-programming/polygon-map-generation/) — see that write-up for the source technique on both items below.
  - **Water bodies**: `water-bodies.ts` (`classifyWaterBodies()`) flood-fills `tectonics.isLand`'s water cells into connected components; the single largest component is `'ocean'`, every other component is `'lake'`. Before this, `isLand` had no notion of connectivity — a landlocked water pocket and the open ocean were both just "not land", and the only `'lake'`-adjacent concept was `rivers.ts`'s `lakeCorners` (river-termination local minima, not an actual water-body classification). `computeBiomes()` now takes `waterBodyKind` and emits a distinct `'lake'` biome (frozen water of either kind still reports `'ice_cap'`, unchanged); moisture's ocean-seeded BFS already treated all `!isLand` cells as sources so it needed no change. This is a pure classification pass over existing water, not depression-filling — it won't create a new lake by filling an interior elevation minimum that sits above sea level (a separate feature, not done here). 3/3 new unit tests passing (`water-bodies.spec.ts`): every water cell gets classified and adjacent water cells always share a kind (connectivity invariant), a hand-flipped single land-locked cell classifies as `'lake'` while a real ocean cell exists elsewhere, and an all-land graph returns all-null. `/cell-planet-lab`'s biome color mode and 3D preview now render lakes in a distinct blue from ocean.
  - **River flow accumulation**: `traceRivers()` now tracks a running per-corner visit count as each source's downhill walk proceeds. Downhill stepping is a pure function of the current corner (always the single lowest neighbor), so once two sources' paths converge on a corner they are identical from there on — counting visits per corner is therefore exactly the discharge at that corner, no separate merge-detection needed. Traced in two passes (find every path's corner indices and finalize the counts, then read the counts back per path) so a source visited later in iteration order can't leave an earlier path's recorded flow stale. `IPlanetRivers.riverFlow: number[][]` is a new field parallel to `riverPaths`; `riverPaths` itself is unchanged so the M2/M3 river tests needed no changes. The `/cell-planet-lab` 2D unwrap's rivers-mode overlay now strokes each river segment at `baseWidth * (1 + 0.5·sqrt(flow−1))`, so merged rivers visibly widen downstream (canvas 2D supports per-segment `lineWidth` cheaply). The 3D preview's river overlay is still a uniform-width `LineSegments` — `LineBasicMaterial`'s `linewidth` is ignored by most WebGL backends, so matching this in 3D needs three.js's fat-line addon (`Line2`/`LineMaterial`) or a width-varying ribbon mesh; **deferred**, not done here. 1 new unit test passing (`rivers.spec.ts`): flow is ≥1 everywhere and never decreases going downstream along any single path, exercised with a raised `sourceCount` to guarantee at least one actual merge.
  - **Considered and explicitly not taken from the article**: *noisy edges* (jittering polygon/coastline/river boundaries within a constrained quad so they don't read as mechanically straight) — real technique, but it's cosmetic stylization, not a fix for the mesh-resolution-driven coastline/overlay mismatch already tracked elsewhere in this doc; revisit for visual polish once the M4 rendering milestones land, not before. *Moisture fed by rivers/lakes* (the article's "distance to fresh water" includes rivers) — ours only seeds from `!isLand` cells; adding rivers as a moisture source creates a pipeline-ordering problem since moisture is computed *before* rivers (river sourcing reads moisture), so it needs a two-pass moisture computation — bigger change, not done here.
- **M1.1 — Ridge-line mountain shaping, decoupled from planet/cell scale — done (2026-08-27)**: prompted by Bruno observing that mountains formed as a blob clumped at continent centers instead of a line, and that shrinking the planet made whole continents turn mountainous. Root cause (confirmed by reading `elevation.ts`/`plate-boundaries.ts`): `computeElevation()`'s `spreadInfluence()` pushed every boundary edge's uplift outward `boundaryFalloffRadius` (3) graph-hops, decaying per hop — and a mountain *range* is many adjacent convergent edges in a row, each spreading independently, so their overlap compounded into a blob covering most of a continent's interior. Being hop-count-based (not physically scaled), the blob's footprint relative to a whole landmass grows as cell count/planet size shrinks — exactly the scale-dependence Bruno was seeing. Separately, `computeBiomes()`'s `'alpine'` classification rode a global elevation-percentile threshold on that same smoothed field, so the *visual mountain* and the *actual tectonic ridge* (already available as `ridgeCellIds`, unused for this) were only coincidentally related.
  - **Fix**: continent-continent convergent (ridge) *and* ocean-ocean convergent (island-arc — the same real-world phenomenon, a line of collision uplift, just underwater) edges now use a new `ridgeFalloffRadius` param (default 0 — no spread past the boundary's own two cells) instead of the general `boundaryFalloffRadius`. Subduction and rift/mid-ocean-ridge contributions are untouched (those are realistically broad regional features, e.g. the Andes' uplift belt, not a single line). `computeBiomes()` now takes `ridgeCellIds` and classifies them `'alpine'` unconditionally (still gated behind the existing frozen/land checks), on top of — not replacing — the elevation-percentile check, so ridge cells always read as a mountain line tracing the actual boundary geometry regardless of cell density or planet size, while other compounded-tall terrain (e.g. a subduction zone) can still qualify via the percentile path.
  - **Follow-on fix, same day**: with ridge/island-arc spread removed, `landRelief` (max land elevation minus sea level) dropped to its realistic un-inflated scale — it turned out several other biome tests were implicitly relying on the old inflated scale. `computeBiomes()`'s `flatnessSlopeFraction` default (meadow-vs-hills cutoff) was recalibrated from 0.08 to 0.2 after empirically sampling the actual normalizedSlope distribution across seeds (old default was stricter than the *minimum* slope in the new data — meadow cells had become impossible to produce at all). Two hardcoded test seeds (`biomes.spec.ts`'s meadow/desert tests) were swapped for seeds verified (via a throwaway `tsx` sweep script, not committed) to still produce that biome under the corrected model — not a workaround, since the land/water *pattern* legitimately shifts with different elevation shaping even at the same `targetLandFraction`.
  - **Follow-on fix (2026-08-28) — coastal deserts were structurally impossible**: a `biomes.spec.ts` test asserted "deserts never touch the coast" as an invariant — Bruno correctly called this out as backwards (Atacama, Namib, Baja California, Australia's west coast are all real coastal deserts) and pointed out that deleting the test doesn't fix the actual generation bug it had encoded. Root cause: `climate.ts`'s moisture was a pure BFS distance-from-ocean field, so a coastal land cell (always 1 hop from an ocean source) was structurally guaranteed to be well above the dry-moisture threshold — deserts could *only* form far enough inland to exhaust that field, regardless of any other condition. Fixed by adding a second, independent dryness term: `aridBeltCenter`/`aridBeltWidth`/`aridBeltStrength` model Earth's subtropical high-pressure belts (~20-30° latitude both hemispheres, Hadley-cell descent suppressing rainfall independent of distance from water) as a triangular multiplier over `|y|` applied to land moisture after the distance field. This is the same real phenomenon that drives Bruno's actual coastal-desert examples, and it composes with the existing field rather than replacing it — inland deserts (dry via distance) still form same as before. Verified empirically: 79/80 swept seeds now produce at least one coastal desert cell, while still producing plenty of interior ones. The old (wrong) invariant test was removed and replaced with `biomes.spec.ts`'s "deserts can be coastal, not just interior" test, which positively asserts at least one coastal desert exists at a fixed seed rather than asserting the old absence.
  - **Considered, not done — the rest of desert/rainfall sophistication (Bruno's #1 ask)**: the arid-belt fix above only addresses "deserts were structurally confined to continent interiors" — it's still an isotropic, static, one-shot field, not the fuller ask. Proposed direction for the rest: a latitude-banded prevailing-wind model (3-cell atmospheric circulation — trade winds/westerlies/polar easterlies, a known cheap pattern) walking moisture downwind instead of isotropically, depleting extra moisture crossing a ridge cell (true orographic rain-shadow, distinct from the arid belt's latitude-only effect). For the "dynamic weather / desertification as a consequence of play" half of the ask: that implies moisture becoming a live, mutable field (not a one-shot generation output) — worth designing this pass so a later runtime layer can perturb it (similar shape to the sparse terrain-edit override layer planned for M4e), but actually simulating weather live is a separate, bigger feature, not scoped here.
  - **Considered, not done — volcanoes (Bruno's #4 ask)**: cheap once ridge-line data exists, since the boundary types are already classified — ocean-continent subduction (continental-side uplift, real-world analog: Andes/Cascades volcanic arcs), ocean-ocean convergence (island arcs, now already treated as a sharp line above), and oceanic divergence (mid-ocean ridges) are all classic volcanism sites. A sparse `volcanoCellIds` (same shape as `ridgeCellIds`) tagging a subset of those boundary cells is the natural next step — not done this pass.
- **M4 — Rendering (reshaped 2026-08-26, see Layer 2)**: split into small, independently-useful steps instead of one big spike — each is a day-ish bite, and nothing later throws away earlier work.
  - **M4a — Canonical elevation function — done**: `sample-elevation.ts` (`sampleElevation()`, `findCellAt()`, `cellCornerElevation()`) in `worldgen` — pure, barycentric-blended from the containing cell's own elevation and the 2 corner elevations of whichever fan wedge the query point falls in (corner elevation = average of the 3 cells meeting there, same formula `rivers.ts` already used internally). `findCellAt()` is nearest-site-by-dot-product, which is exactly Voronoi cell membership for these graphs — brute-force O(cellCount), fine for arbitrary/occasional queries (future collider patches) but too slow to call per mesh vertex at high cell counts. `cellCornerElevation()` is exported separately as the O(1) path for callers (mesh/chunk builders) who already know which cell/corner they're touching. 5/5 new unit tests passing (56/56 total): exact at cell centers, matches the corner-average exactly at corners (continuity across cell borders), `findCellAt` round-trips a cell's own center, deterministic, stays within the local min/max elevation. The `/cell-planet-lab` 3D preview mesh now displaces every vertex via the O(1) direct lookup (not `sampleElevation()`'s search) and exposes the exaggeration factor as a live "elevation ×" slider (0–15%, default 2%) instead of a fixed constant — a flat-shaded low-poly mesh reads as a lumpy asteroid well before the terrain reads as "tall", so this needed to be tunable, not guessed once. Cell-count slider raised 500 → 3000 now that per-vertex elevation lookup is O(1) instead of O(cellCount). No chunking/LOD/collider wiring yet — that's M4b onward.
    - **Follow-up fix (same day)**: displaced vertices are now clamped to sea level from their owning cell's land/water side (`isLand[cellId] ? max(e, seaLevel) : min(e, seaLevel)`) — without it, a corner's raw value is a plain 3-cell average (`cellCornerElevation()`), so an ocean cell next to a mountain got one corner dragged above the elevation of unrelated flatter land cells elsewhere, i.e. water rendering visibly higher than land. This is a placeholder (land/water still share one blended mesh) — the real fix is a separate flat ocean shell, which is M4b/e territory, not done here. Also swapped `<orbitControls>` for `<raycastOrbitControls>` (pivots wheel-zoom/rotate on the actual displaced surface under the pointer, not a flat distance guess), set `near` to `0.001` (was the default `0.1`, ~10% of planet radius — clipped when orbiting close to the surface), and added a "surface-up camera" toggle that re-levels the orbit camera's up vector to the current radial direction every tick instead of a fixed world-Y up (avoids the pole gimbal when orbiting at high latitude).
  - **M4b — Chunking — done (2026-08-28)**: `chunking.ts` (`buildPlanetChunks()`, `buildChunkMeshData()`) in `worldgen/core`. Cell id order carries no spatial locality (Fibonacci-spiral + golden-angle placement means consecutive ids are scattered around a latitude band, confirmed by reading `fibonacci-sphere.ts` and `lloyd-relaxation.ts` — Lloyd relaxation smooths positions in place but never reorders/reassigns ids), so `buildPlanetChunks()` groups cells into ~`targetChunkSize`-cell (default 100) contiguous regions by BFS flood-fill over `cell.neighbors`, guaranteeing every chunk is a single connected patch. Each chunk also gets a bounding center + angular radius (max angle from centroid to any member cell's corners) — the bounding volume the deferred horizon/frustum cull below will test against. `buildChunkMeshData()` tessellates one chunk into the same triangle-fan-per-cell layout the M3/M4a whole-planet mesh used (center → corner k → corner k+1), returned as plain typed arrays (framework-free) rather than a `BufferGeometry`. 7/7 new unit tests passing (`chunking.spec.ts`): every cell assigned to exactly one chunk, every chunk is a single connected region, chunk count is roughly `cellCount / targetChunkSize`, deterministic for a given graph/params, bounding volume actually contains every member cell, and `buildChunkMeshData()`'s vertex/element counts line up exactly with the un-chunked tessellation. 63/63 tests passing total. Wired into `/cell-planet-lab`: `rebuildPreviewMesh()` now builds one `Mesh` (draw call) per chunk instead of one for the whole planet — at the default cell count (180) and `targetChunkSize` (100) that's ~2-3 draw calls instead of 1; at 3000 cells it's ~30. Per-chunk vertex/elevation/cell-id caches moved from single flat arrays onto each chunk `Mesh`'s `userData`, so `updatePreviewDisplacement()`/`updatePreviewColors()`/`computeMeshWaterlineDirections()` (waterline extraction) all now loop `previewMeshes` instead of touching one array — no behavior change, same per-vertex math, just re-scoped per chunk. Raycasting (`raycastFocusResolver`, used by `raycastOrbitControls`) now hits `previewMeshes` as a set instead of one mesh. New `chunks` stat added to the lab page's stat strip. No LOD yet (every chunk still renders full-resolution regardless of camera distance) — that's M4c.
  - **M4c — Per-chunk discrete LOD — done (2026-08-28)**: 2 fixed levels per chunk (the milestone's "2–4" range, deliberately starting at the low end — see below), added to `chunking.ts`. LOD0 is the existing `buildChunkMeshData()` (full per-cell detail). LOD1 is new: `buildChunkBoundaryLoop()` walks a chunk's outer edge (an edge `corners[k]→corners[k+1]` of a member cell is a boundary edge iff the cell across it, per `IPlanetGraphCell`'s neighbor/corner indexing, belongs to a different chunk) and chains boundary edges head-to-tail by object identity into a closed loop — cheap and exact because corner positions are shared object references between every cell touching that Voronoi vertex (`dual-cells.ts`), not recomputed per cell. `buildChunkLod1MeshData()` applies that loop extraction not to the chunk as a whole but to **merge groups within it** — contiguous cell clusters grown by the same BFS (`growConnectedGroups()`, now shared with `buildPlanetChunks()`) under a curvature-error budget — and renders each group as one flat polygon over its merged boundary. The key property: a merged polygon's vertices are the *exact same* corner objects a neighboring chunk's LOD0 cells use along that shared border, and their elevations (`cellCornerElevation()`) are mathematically the same `(a+b+c)/3` regardless of which of the 3 surrounding cells computes them — so LOD0-to-LOD1 borders never crack, with no skirt or explicit stitching pass, in either direction, for any neighbor-LOD combination. (Elevations agree to floating-point summation-order rounding, ~1e-16 for typical magnitudes — far below float32 precision, confirmed by a dedicated unit test.) Falls back to full-resolution for any group whose boundary isn't a single simple loop — including a group that owns the entire graph (nothing to seam against), and, since a second same-day fix, any ring-shaped or pinched region, which `buildChunkBoundaryLoop()` now detects by checking that the traced loop consumed every boundary edge (`positions.length === nextByStart.size`) rather than assuming it did and silently emitting a truncated loop.
    - **Same-day bug + fix (1 of 2 — triangulation)**: the first version fanned the boundary loop from a single interior point (the chunk's own centroid) instead of triangulating it properly. A single-point fan only tessellates a polygon correctly when the polygon is *star-shaped* around that point (every boundary point visible from it without crossing an edge) — BFS-grown chunk regions are frequently non-convex "amoeba" shapes (flood-fill has no compactness heuristic), so the centroid routinely wasn't star-shaped-safe, and the fan triangles folded over each other. Visually this looked like sharp radiating spike/pinwheel patterns replacing recognizable terrain at LOD1 (caught by Bruno from a screenshot: "the far ones are unrecognizable... perhaps the geometry is all at the same point"). Fixed by replacing the fan with a real ear-clipping polygon triangulation (`triangulatePolygon2D()`, new in `chunking.ts`) run on the loop projected into a local tangent-plane 2D basis at the chunk's centroid — the projection only decides *which* vertex triples form triangles, output positions/elevations are still the real 3D loop data. Ear clipping has no star-shaped requirement (works for any simple polygon, convex or concave) and needs no interior apex vertex at all, so LOD1 triangle count actually dropped from `boundaryVertexCount` to `boundaryVertexCount - 2`. Locked in with a dedicated test: a concave "L"-shaped polygon (reflex vertex right where a centroid-fan would have broken) triangulated into exactly `n-2` triangles whose summed area exactly matches the polygon's own shoelace area — the check that would have caught the original bug (overlapping fan triangles make that sum diverge from the true area).
    - **Same-day bug + fix (2 of 2 — the actual one)**: the ear-clipping fix above was a real fix to a real bug, but it was not the bug in the screenshot; Bruno re-tested and reported "not much improvement, same looking issue." The genuine defect was never topological — it was that collapsing an entire chunk to a single flat polygon is the wrong LOD, for a reason that renders as garbage rather than as an obviously-too-coarse mesh. **A flat polygon spanning a spherical cap of angular radius `r` sags below the sphere by `1 - cos(r)` at its center.** At this module's chunk sizes (a few tens of chunks per planet, so `r` ≈ 20–35°) that's 0.06–0.18 of planet radius — one to two orders of magnitude past the *entire* terrain relief being rendered (~0.02 at the lab's default elevation scale). Each chunk stopped being a coarse surface and became a chord plate buried inside the planet, so land sank beneath the fixed-radius sea-level shell and the ocean rendered in front of the continents, with the plate's triangles fanning out through it — which is exactly what the screenshot showed. It also explains why the ear-clipping fix changed nothing visible: ear clipping on a near-convex loop *legitimately* produces fan-like radiating triangles, so the corrected triangulation looked identical to the broken one. **Fix**: LOD1 now merges cells into small groups bounded by that sag (`IChunkLod1Params.maxSag`, default 0.002 → cap radius ~3.6°, applied to candidate cells' corners during BFS growth) instead of collapsing the whole chunk. A cell-*count* cap was tried first and rejected by measurement: BFS groups aren't discs (later groups fill the stringy gaps between earlier ones), so an 8-cell cap still measured 0.073 max sag — a stringy 8-cell group spans roughly twice the angle of an 8-cell blob. Bounding the sag directly took it to 0.0067, whose remaining floor is just a single large cell polygon's own curvature. Consequence worth stating plainly: the payoff now scales with resolution rather than being constant. At the lab's default 180 cells one cell already exceeds the budget, so groups bottom out at one cell and LOD1 degenerates to "cell polygons minus their fan centers" (`n-2` triangles per cell instead of `n`, ~⅓ saving); the merge only starts compounding at the high end of the cell-count slider. That is the correct behavior for a geometric-error-budget LOD — it saves more where there is more to save — but it does mean LOD1 is nearly free of benefit at low cell counts, which the previous (broken) version disguised by simply throwing the geometry away.
    - 10/10 new unit tests passing (`chunking.spec.ts`, boundary loop + LOD1 + triangulation): boundary loop closes with ≥3 vertices, every boundary vertex is bit-identical in position (and numerically identical in elevation) to the corresponding corner on whichever neighboring chunk's cell also touches it, `null` when the region owns the whole graph, LOD1 has strictly fewer vertices than LOD0 for a typical chunk, LOD1 vertices sit on the unit sphere, the unbounded-merge fallback matches LOD0 exactly, the two `triangulatePolygon2D()` tests above, and — the regression test for bug 2 — **max sag across every LOD1 triangle stays within `worstSingleCellSag + maxSag` and under a tenth of what the same chunk collapsed whole would produce**, with both bounds derived from the graph and the documented budget rather than tuned to observed numbers. 72/72 tests passing total.
    - **Follow-up (2026-08-28) — feature pinning, so the merge itself stops popping**: even with sag bounded, Bruno reported a live version of the geomorphing problem: switching a chunk from LOD0 to LOD1 could flatten an interior mountain to its surrounding color, make a small island disappear (fully absorbed into a merge group dominated by its water/land neighbors), or shift the rendered coastline, since LOD1 only keeps a merge group's *boundary* vertices — anything interior to a group, however distinctive, is simply gone. A hard "pin everything above a prominence/size threshold" was considered and rejected before writing any code: it regresses exactly where LOD1 matters most, since a genuinely mountainous chunk has most of its cells clear a fixed bar, so LOD1 would do nothing right where the triangle count is highest. Landed instead as `salience.ts` (`computeCellPins()`) — a **per-chunk top-K budget**, not a threshold, ranking three independent categories and pinning only the winners to always render as their own single-cell polygon at both LODs: peaks by *prominence* (elevation minus neighbor average, so a uniformly tall plateau doesn't all qualify — only what genuinely sticks out from its immediate surroundings does), coastline features by curvature (each `extractCoastlines()` loop vertex compared against the midpoint of neighbors a few steps along the same loop — a straight shoreline scores near zero, a cape or bay mouth scores high), and islands (land flood-filled into connected components, the single largest excluded as "mainland", the rest ranked by cell count and pinned **whole or not at all** — partially pinning an island would still show its unpinned half flattening, the exact popping this exists to prevent). `growConnectedGroups()` (shared by `buildPlanetChunks()` and LOD1's merge) takes an optional `pinned: Uint8Array`: a pinned cell is skipped as a candidate neighbor by every other group and never expands its own frontier if it becomes a seed, so it's always a singleton group — `buildPlanetChunks()` passes nothing and is unaffected (pinning is LOD1-only). Computed once per graph in `/cell-planet-lab`'s `rebuildPreviewMesh()` (not per chunk, not per frame) and threaded into every chunk's `buildChunkLod1MeshData()` call as `{ pinned }`. 6 new unit tests (2 in `chunking.spec.ts` for the `growConnectedGroups()` pinning mechanic itself, 4 in the new `salience.spec.ts` for `computeCellPins()`'s peak/budget/island-ordering/mainland-exclusion behavior), 78/78 tests passing total.
    - **Follow-up (2026-08-28/29) — a "highlight pinned cells" debug toggle, and a real bug it surfaced**: Bruno tested the pinning feature and reported the on/off toggle produced no visible difference ("i see mountains disappear, i see islands disappear... not sure how i can test this properly"). A throwaway `tsx` diagnostic against the lab's actual default settings confirmed `computeCellPins()` does pin real, non-trivial cell counts (22–183 depending on cell count 180–3000) — so the mechanism wasn't inert, just unverifiable by eye through an indirect A/B toggle at a ~90-cell-chunk budget of 2 peaks/2 coastal/12 island cells. Added a second checkbox, `highlightPins`/`toggleHighlightPins()` in `cell-planet-lab-page.component.ts`, that force-recolors every pinned cell bright magenta on the live mesh regardless of map mode, so pinning can be checked directly instead of inferred. Bruno re-tested with it on and confirmed an isolated peak (no neighboring mountains) does render magenta — pinning itself works — but also reported that same peak's *color* still visibly changed from white to green between LOD0 and LOD1 in plain elevation mode, and that other areas he expected pinned showed no magenta.
      - **The white→green report was a real, separate bug, found by reading `buildChunkLod1MeshData()`**: a pinned cell is correctly forced into its own singleton merge group (never absorbed), but that group still went through the same flattened boundary-loop + ear-clipping path every merge group uses — which only carries *corner*-blended elevations (`cellCornerElevation()`, the `(a+b+c)/3` average with neighbors) and has no vertex at the fan center at all. The fan center's elevation is the cell's own raw, unblended `elevation[]` value — and peak-prominence pinning (`elevation[cell] − average(neighbor elevations)`) specifically selects cells where that raw value sits well above the corner blend, since that's what "prominent" means. So the very peaks pinning exists to protect were exactly the cells whose LOD1 rendering most understated their height and color, even though they were correctly kept as their own polygon — "pinned but still flattens/dims" rather than "not pinned at all". **Fix**: `buildChunkLod1MeshData()` now special-cases a pinned singleton group to render with the same fan tessellation `buildChunkMeshData()` uses at LOD0 (center vertex included) instead of the boundary-only polygon, making a pinned cell's LOD1 geometry and elevation byte-identical to LOD0 rather than merely "kept as its own shape." The existing pinning unit test (previously written with all-zero elevation, so it couldn't have caught this) was rewritten to use varying per-cell elevation and assert LOD1 output matches a direct LOD0 fan build vertex-for-vertex, including the fan center. 78/78 tests still passing (no new tests added, one rewritten).
      - **The "expected pink, saw none" report is expected budget behavior, not (yet confirmed to be) a bug**: with only 2 peaks + 2 coastal + 12 island cells protected per ~90-cell chunk, a chunk can easily contain a feature a human would call prominent that still doesn't clear the local top-K — this is the tradeoff the budget design explicitly accepts (see the threshold-rejection reasoning two paragraphs up), not a defect by itself. Not yet investigated further — worth checking with the highlight toggle whether raising `maxPeaksPerChunk`/`maxCoastalFeaturesPerChunk`/`maxIslandCellsPerChunk` (or shrinking `targetChunkSize` so each chunk has fewer competing features) fixes the specific spots Bruno flagged, before assuming it's a scoring bug.
    - Wired into `/cell-planet-lab`: `rebuildPreviewMesh()` now bakes both LOD meshes per chunk up front (so switching is just a `.visible` flip, no geometry work at runtime); a new per-frame `updateChunkLod()` (added to the existing tick subscription alongside `updateCulling()`) measures camera distance to each chunk's centroid against a new "LOD distance" slider and shows exactly one of the two meshes per chunk. A new "LOD split" stat reports the live near/far chunk count, and the existing "chunk colors" debug toggle (flat hash color per chunk) now keys off `mesh.userData.chunkId` instead of array index so both of a chunk's LOD meshes agree on color as LOD swaps. Coastline waterline extraction (`computeMeshWaterlineDirections()`) was restricted to LOD0 meshes only, so the rivers-mode coastline overlay stays stable instead of visibly changing shape as chunks swap LOD under camera movement.
    - **Scope note**: only 2 levels, not the milestone text's full "2–4" range. The merge-group rewrite makes further tiers cheaper than the earlier version of this note claimed — a 3rd level is now just a larger `maxSag`, and every tier stays crack-free against every other for the same reason tier 1 does (all merged polygons are built from unmodified shared corner objects, so no tier ever invents a vertex another tier doesn't have). What still isn't free is that a coarser tier only helps if its budget is large relative to a cell, which at low cell counts it can't be — so more tiers are worth adding alongside the higher-resolution work (M4d/M4e), not before it. Deferred, not attempted this pass.
    - **Still not implemented**: visibility culling (2026-08-28 note, unchanged from before this pass) — per-cell culling every frame doesn't scale, but testing ~N chunk bounding volumes (`buildPlanetChunks()` already computes center + angular radius) against the camera frustum and the planet's horizon plane does; a chunk fully over the horizon or outside the frustum could be skipped whole (both meshes hidden, not just LOD-swapped). Expect roughly half the chunks culled by the horizon alone on any given frame (far side of the planet). Natural next addition to `updateChunkLod()`'s per-chunk loop, reusing the horizon-test gap already flagged in M0's lab-page note above (needs a proper altitude-aware horizon test, not the flat dot-product cutoff that page currently uses) — not done in this pass, which focused on the LOD selection itself.
  - **M4d — Local collider patches — done (2026-08-29)**: high-res mesh patches sampled from `sampleElevation()` near vessels, independent of visual LOD/camera. Proves the camera/physics decoupling.
    - **Scoping notes (2026-08-29), written before implementation**: three questions came up from BSP before writing code — captured here so the implementation below didn't re-derive them.
      - **Does this need surface visual detail (M4c's deferred close-up noise) to land first? No.** `sampleElevation()` is already density-independent (Layer 2) — it can be sampled at arbitrary resolution regardless of what LOD tier the visual mesh is showing. Colliders will just be geometrically smooth (no micro-bumps) until a detail/override layer exists; the architecture already reserves room for that later (same override-layer seam M4e is designed around), it isn't a blocker.
      - **Does a planet-size/scale-tier selector need to exist first? No, and it must not be `worldgen`'s.** BSP already has almost exactly this (`celestial/bodies/world-size-presets.ts`'s `WorldSizeTier`, 0.001×–1× Earth radius), but it's wired only to the old CDLOD/celestial pipeline and, more importantly, is a BSP/consuming-app concept. Per [024](./024_sublibrary_reusability_boundaries.md) (written from this exact question), `worldgen/core` stays dimensionless. **Revised while implementing, below**: rather than threading a `radiusM` parameter into the patch builder itself, M4d actually followed M4b/M4c's own established precedent more literally — `buildColliderPatch()` returns unit-sphere directions and unitless elevations, exactly like `buildChunkMeshData()` does, and the meters conversion happens one layer further out, in whatever mesh/heightfield adapter a consuming app writes (mirroring how `/cell-planet-lab` itself displaces the visual mesh: `radius = 1 + elevation * scale`, entirely in app code). Slightly more conservative than the original note's "takes a plain `radiusM`", and more consistent with the rest of this module.
      - **Is BSP's existing collider-streaming work reusable, or from scratch? The policy layer is reusable; the addressing scheme is not.** BSP's `TerrainColliderStreamer`/`terrain-collider-selection.ts` (budgeted per-frame builds, critical-footprint bypass, gapless dirty-rebuild-swap via build-new-before-destroy-old) is a battle-tested *pattern* worth reusing as-is for M4d's streaming policy. But its patch addressing (`IPlanetPatchAddress` — quadtree face/level/x/y) is CDLOD-specific and doesn't fit a Voronoi cell graph. M4d needs a new local high-res patch-mesh source, sampling `sampleElevation()` directly rather than a quadtree address, living inside `worldgen` (generic); BSP keeps owning streaming policy/budgets/radii in app code, same split as today.
    - **Core implementation — done (2026-08-29)**: `collider-patch.ts` (`buildColliderPatch()`, `colliderPatchIndices()`) in `worldgen/core`. A patch is a regular `sampleCount x sampleCount` grid on the tangent plane at an arbitrary query direction (a vessel's ground-track point, not a cell or chunk), gnomonic-projected onto the sphere, then sampled — free to pick a resolution and extent completely independent of `cellCount`, chunk boundaries, or the chunk's current LOD tier, which is the actual property M4d exists to prove. Output shape matches `buildChunkMeshData()`'s convention exactly (unit-sphere `directions` + unitless `elevations`, both flat typed arrays, no Three.js/Jolt coupling) rather than emitting a metrics-ready mesh — see the scoping-note revision above.
      - **Why cell lookup needed its own function, not just repeated `sampleElevation()` calls**: `sample-elevation.ts`'s own `findCellAt()` doc comment already flagged this exact caller — "callers on a hot per-frame path should batch or cache rather than call this per-vertex per-tick" — as the reason its brute-force O(cellCount) scan wasn't going to be good enough for a dense local grid. Added `findCellNear()` (a Delaunay/Voronoi walk: dot-product-nearest-site is unimodal across the neighbor graph for a convex site set, so greedily stepping to whichever neighbor improves the dot product further always converges on the true containing cell) and `sampleElevationNear()`, seeded row-by-row from the previous grid vertex's resolved cell. Measured on a 3000-cell graph (throwaway `tsx` script, not committed): a 33×33 patch (1089 vertices) built in 3-5ms warm, a 17×17 patch averaged 0.8ms across 20 patches at varying locations — independent of `cellCount` after the first vertex's cold lookup, cheap enough for a per-vessel-move rebuild under a streaming budget.
      - **A real bug caught by testing, not by inspection**: the first version of `colliderPatchIndices()`'s winding-flip check had its comparison backwards (`dot(faceNormal, center) < 0` where it needed to be `> 0`), which produced a buffer where **100% of triangles wound inward** — every single quad, not a marginal/precision case. The unit tests (`collider-patch.spec.ts`'s "every triangle winds outward" check) plus a throwaway verification script both caught it immediately (`badWinding=128` out of 128 triangles); fixed by flipping the comparison. Left as a note here because it's exactly the kind of directional/sign bug that reads as plausible on inspection and only shows up once you actually check face normals against real 3D positions — the same category of bug `buildChunkLod1MeshData()`'s own winding-correction comment (M4c, above) was written to guard against, and the reason both places use the identical "check one triangle, flip the whole buffer if wrong" technique rather than trusting the index math by construction.
      - 12 new unit tests (`collider-patch.spec.ts`: unit-sphere vertices, center-vertex exactness, resolution independent of `cellCount`, angular-bound sanity, determinism, degenerate-input rejection, index-count/bounds, outward winding; `sample-elevation.spec.ts`: `findCellNear`/`sampleElevationNear` match `findCellAt`/`sampleElevation` from any starting hint, including a coherent row-by-row walk). `npm run test:triangular-engine:worldgen`'s Karma target was unrunnable at the time (an unrelated in-flight session had `clouds`/`water` mid-edit with real compile errors in their own spec files, sharing the same test bundle) — verified instead via `ng build triangular-engine` (typechecks every entry point including `worldgen`, excludes specs, built clean) plus a throwaway `tsx` script exercising the new functions directly against real generated graphs, the same fallback this doc's own M2.5/M4c entries have used before when a quick check was needed outside the full harness.
    - **Lab wiring — done (2026-08-29)**: `/cell-planet-lab` gained a "collider patch (M4d)" toggle plus "patch resolution" (5-33, step 2) and "patch width" (1-20°) sliders. While on, `updateColliderPatch()` (run every tick) raycasts straight out from the camera against the live `previewMeshes` — the same "what is the camera looking at" stand-in `raycastFocusResolver` already uses for orbit-pivot placement — and rebuilds the patch (`rebuildColliderPatch()`) once the hit direction has drifted far enough from the last build to matter, rendered as a magenta wireframe mesh via `colliderPatchMaterial`. `onElevationScale()` now also calls a new `applyColliderPatchDisplacement()`, which re-scales the cached raw patch (direction + elevation, kept around specifically for this) without resampling — the same split `updatePreviewDisplacement()`/`updateRiverOverlayDisplacement()` already use between "resample" (rare, heavier) and "reposition already-sampled vertices" (cheap, safe every slider tick). Verified via `ng build demo-app`, which compiled the `cell-planet-lab-page-component` chunk clean; not yet manually driven in a browser this pass. This proves the *rendering* half of M4d's camera/physics decoupling claim — the patch visibly stays fine-grained under the probe point regardless of the underlying chunk's LOD tier or cell density — but not the *physics* half.
    - **Jolt wiring — done (2026-08-29)**: `/cell-planet-lab`'s "physics test (M4d)" toggle wraps a `<joltPhysics>` block around the same live patch: a static `joltRigidBody`/`joltMeshShape` built straight from `buildColliderPatch()`'s geometry, plus a dynamic ball (`joltSphereShape`) dropped onto it under gravity pointed at the patch's own local "down" (radially toward the planet center). "Drop ball"/"move here" increment a `dropAttempt` counter that an `@for (attempt of [dropAttempt()]; track attempt)` block keys off of, forcing Angular to destroy+recreate the ball's `joltRigidBody` so it actually respawns at `patch.dropPosition` rather than resting wherever the last drop left it (this intentionally trips Angular's NG0956 "expensive track" warning — expected here, not a bug).
      - **A real, separate bug found along the way**: this route (`/cell-planet-lab`) crashed on load with `TypeError: Jolt.BodyActivationListenerJS is not a constructor` the first time `<joltPhysics>` was added — `JoltPhysicsComponent`'s `activationListener`/`contactListener` fields are class-field initializers, which per JS semantics run before the constructor body regardless of source order, so they raced Jolt's async WASM `load()`. Every other Jolt-using route already guards against this with a `canActivate: [() => import('triangular-engine/jolt').then(({ JoltPhysicsService }) => JoltPhysicsService.load().then(() => true))]` on its route entry; `cell-planet-lab`'s route was just missing it (never having needed Jolt before this milestone). Fixed by adding the same guard in `app.routes.ts`.
      - Bruno confirmed in-browser (2026-08-29): "yeah it works the ball drops, the ball follows the terrain and rolls." M4d is fully proven end-to-end — camera/physics decoupling (rendering) and actual collision response (physics) both demonstrated on the same patch.
  - **M4e — Terrain-edit rebuild path — done (2026-08-29)**: sparse override layer on top of `sampleElevation()` (player edits as save data) + rebuild-affected-chunk(s)-only on edit. Proves flatten/dig is affordable before base building is built on top of it.
    - **Core — `terrain-edits.ts`** in `worldgen/core`: `ITerrainEditLayer` is a plain `Map<cellId, absoluteElevation>` — sparse and directly save-data-shaped (a planet with a handful of edits serializes as a handful of pairs, not a copy of the whole terrain). `buildEffectiveElevation(base, edits)` materializes the array every existing consumer already accepts unchanged — `sampleElevation()`, `buildChunkMeshData()`, `buildChunkLod1MeshData()`, `buildColliderPatch()` needed zero changes, exactly the "bridge function" role `sample-elevation.ts`'s own M4a doc comment named the edit layer for. Returns the base array itself (no copy) when there are no edits. `flattenCells()`/`digCells()` write absolute values, not deltas — `digCells()` reads each cell's *current effective* elevation before offsetting, so repeated strokes on the same spot compound instead of each one re-offsetting the untouched original. `cellsWithinHops()` (plain BFS over `cell.neighbors`) gives the brush its radius.
    - **The subtlety that actually decides the rebuild scope**: editing cell A doesn't only change A's own rendered geometry — `cellCornerElevation()` blends a corner from the 3 cells that meet there, so it also moves every corner A shares with its neighbors, changing *their* fan geometry even though their own `elevation[]` entry never changed. Rebuilding only the edited cell's own chunk would leave a visible crack at that boundary. `affectedCellIds()` captures this exactly: edited cells plus their direct neighbors, no more; `affectedChunkIds()` maps that set through `chunkIdByCell` to the (almost always 1-2) chunks that actually need new geometry.
    - **Lab wiring**: `/cell-planet-lab` gained a "terrain edit (M4e)" panel — flatten/dig mode buttons, a brush-radius slider (`cellsWithinHops()`'s hop count), a dig-delta slider, "apply at camera look-at" (reuses `raycastPlanetDirection()`, the same camera-probe M4d's physics test uses), and "reset edits". Flatten levels the whole brush to the clicked cell's own current elevation (so the click picks the target height); dig offsets by the delta slider, compounding on repeated clicks. `rebuildPreviewMesh()` now always reads geometry through `buildEffectiveElevation(tectonics.elevation, terrainEdits)` rather than `tectonics.elevation` directly (a no-op array reference when unedited); a new `rebuildAffectedChunks()` disposes and rebuilds only the chunks `affectedChunkIds()` names, reusing the same per-chunk mesh-build path `rebuildPreviewMesh()` uses (factored into a shared `buildChunkLodMesh()` so the two can't silently diverge). Two new stats — "edited cells" and "chunks rebuilt (last edit)" — make the actual claim visible rather than asserted: a throwaway verification run on a 1500-cell/16-chunk planet showed a single-cell edit touching 8 cells (1 edited + 7 neighbors) and rebuilding exactly 1 of 16 chunks. The edit layer is cleared on every `regenerate()` (a new graph means the old cell ids are meaningless against it).
    - **Follow-up, same day — world size was invisible, and it matters more than M4d's scoping notes suggested**: Bruno tried the flatten/dig tool and reported that at the cell-count slider's high end, one brush click reads as "adding an entire new continent" — and asked, again, for a size selector, after M4d's scoping notes (above) had concluded worldgen didn't need one. Re-examined rather than repeated: that conclusion was correct but narrower than it reads — `buildColliderPatch()`/`sampleElevation()` sample *continuously*, so their resolution is independent of `cellCount` and needs no size concept to work correctly. `ITerrainEditLayer` is not continuous — it's keyed by `cellId`, so its smallest possible edit is exactly one Voronoi cell, whatever size that turns out to be. And nothing in `/cell-planet-lab` had ever set a real-world radius at all, so there was no way to tell what size a cell — or a brush — actually was. Fixed by adding a display-only `worldSizeTier` picker (mini/small/medium/large/extra-large, reusing BSP's own parked tier table from `docs/v3-planning/plans/world-size-presets.md` rather than inventing new numbers) plus two new stats: **avg cell size** (mean angular spacing between neighboring cell centers × the chosen tier's radius) and **last edit footprint** (the actual brush's angular extent × the same radius). Changing the tier relabels instantly, no resample.
    - **What the numbers actually show — a real limitation, not just a missing label**: a throwaway `tsx` check across the cell-count slider's range confirms the concern is structural, not cosmetic. At the default 180 cells, avg cell size ranges 1.8 km (mini tier) to 1,840 km (extra-large) — literally continent-scale at the high end, exactly as reported. Pushing `cellCount` to the slider's max (3000) still leaves cells at 0.5 km even on the *smallest* (mini, ~asteroid-scale) tier — nowhere near single-cell precision for a landing pad or building footprint, and cranking `cellCount` further to chase that doesn't scale (a whole-planet Voronoi graph dense enough for meter-scale cells is computationally infeasible). **This means M4e's flatten/dig, as built, is a landscape-editing tool, not a base-building one** — its resolution floor is one cell, and one cell cannot be shrunk to human/vehicle scale without abandoning the whole-planet-graph approach. Making the floor *visible* (this follow-up) was the correct immediate fix; it does not raise the floor. A real building-scale edit tool needs a mechanism that's resolution-independent the way `buildColliderPatch()` already is — e.g. a local stamp/foundation layer sampled continuously and composited over the cell terrain, not a `cellId`-keyed override — which is new scope, not a tuning knob on what exists today. Not started; flagged here rather than silently left implicit in the "affordable" framing above, which was true about rebuild *cost*, not about achievable *precision*.
    - 12 new unit tests (`terrain-edits.spec.ts`): effective-elevation fallback/override, `buildEffectiveElevation`'s no-copy-when-empty and copy-on-edit behavior, flatten/dig/clear semantics (including dig's compounding-on-repeat), `affectedCellIds`'/`affectedChunkIds`' neighbor-expansion and chunk-mapping, `cellsWithinHops`' hop-0/hop-1/monotonic-growth/determinism, and an integration pair confirming an edit both samples correctly at its own cell center and changes sampled elevation at a shared corner with an unedited neighbor (the exact case the rebuild-scope subtlety above is about). Karma's shared spec bundle was unrunnable at the time (same unrelated in-flight `clouds`-spec breakage M4d's entry already documented) — verified instead via `ng build triangular-engine` (clean) plus a throwaway `tsx` script exercising every function against a real generated graph (`ng build demo-app` also confirmed the lab-page wiring compiles, `cell-planet-lab-page-component` chunk built clean).
    - **Second follow-up, same day — the tier picker was still just a label; now it's real, and M4d's physics half moved out**: Bruno pushed further than the display-only fix above — "match the size of the planet when I pick those values" — and, asked to choose between a small edit-brush label fix or genuinely rescaling the render, picked the latter and added scope: split the ball-drop physics demo into its own lab, and use it to prove out real double-precision Jolt positions ("we do have floating point code in triangular specifally for this... yes I want to be able to support the real scales"). Confirmed before building: `jolt-rigid-body.component.ts` already constructs `Jolt.RVec3(...)` straight from its plain `[position]` input (ordinary JS numbers — already full double precision, no float32 downcast anywhere in that path), and the workspace pins a local double-precision `jolt-physics` fork — so real planetary-offset physics needed zero jolt-module changes, just a lab that actually positions things that way. Krakensbane-style origin rebasing (`docs/PLANETARY-SCALE-PHYSICS.md`/`krakensbane-check.md`) remains unbuilt and wasn't needed here (one patch, one ball, close together, both just far from the origin).
      - **`cell-planet-lab`**: `worldSizeTier()` now drives a real `effect()` that sets `root.scale.setScalar(planetRadiusM())` — `root` is the sole parent of every rendered piece (preview chunks, ocean, sites/edges, river/coastline overlays), confirmed by grep before relying on it, so one transform rescales everything together, no per-mesh changes. Camera framing (`cameraPosition`), `near`/`far` (now `0.5`/`500000000`, matching `cdlod-planet-lab`'s proven real-scale recipe — `logarithmicDepthBuffer` was already on), and `lodDistance`'s one camera-distance comparison in `updateChunkLod()` all now scale by `planetRadiusM()` too; raycasting and the M4e edit brush needed no changes (already scale-invariant unit-direction math). `sitesMaterial.size` also scales, or the graph-overlay points would shrink to invisible specks once `root` scales up. The M4d ball-drop demo (`showColliderPhysics`, `physicsPatch`, `dropAttempt`, the `<joltPhysics>` block) was removed from this page entirely — moved below. The rendering-only collider-patch debug overlay (`showColliderPatch`) stayed.
      - **New: `/planet-physics-lab`**: a dedicated, deliberately smaller lab (no chunk LOD/culling, no map canvas, no M4e editing) — generate a planet, pick a world-size tier (now shared with `cell-planet-lab` via a new `WorldSizeTier`/`WORLD_SIZE_TIER_RADIUS_M`/`formatDistanceM` export in `worldgen/render/world-size-tiers.ts`, so the two labs can't drift on what "medium" means), click to place a `buildColliderPatch()` physics test, drop a ball. Ported near-verbatim from the old M4d code, with the one real change: the static patch's geometry and the ball's `[position]` are built directly in real meters (`planetRadiusM() * (1 + elevation * scale)`), not a unit-sphere fraction — these Jolt bodies are declared as scene siblings of `root`, not children of it, so unlike the terrain mesh they get no scale transform for free and must carry real magnitude themselves. Ball radius/drop height became fixed real constants (1 m / 5 m) independent of world size, replacing the old unit-sphere-fraction constants that had quietly worked out to a ~9 km ball at medium tier and were never noticed because nothing had ever been rendered at real scale before. The route's `JoltPhysicsService.load()` `canActivate` guard moved from `cell-planet-lab` (which no longer touches Jolt) to this page.
      - Verification: `ng build triangular-engine --configuration development` (new `worldgen/render/world-size-tiers.ts` entry, clean) and `ng build demo-app --configuration development` both passed for every file this change touched — confirmed by grepping the build log for each changed path, since the overall `ng build demo-app` exit code was non-zero from an unrelated, already-uncommitted in-flight `cloud-puffs-lab` breakage (same class of shared-tree issue this runbook has hit before with Karma's spec bundle; not touched, per the workspace's git-safety rule about other agents'/the human's in-flight work). Bruno's own in-browser check (standing practice for this lab) is the remaining step: confirm the rendered planet visibly grows/shrinks across tiers in `/cell-planet-lab`, and confirm the ball actually rests without jitter/sinking/tunneling at every tier — especially "extra-large" — in `/planet-physics-lab`.
  - Deferred, not scheduled: per-cell adaptive LOD scoring (view-angle/horizon weighting, biome-importance budgets) — only revisit if a real case (large planets, close orbital flythroughs) proves discrete per-chunk LOD insufficient.
- **M5 — Write-up & decision**: record what M4a–e proved (and whether the deferred adaptive-LOD item is actually needed), then scope integration (BSP, scatter, save format for terrain edits) as a follow-up doc.

## Non-goals (for this POC)

- No integration with CDLOD, `celestial`, scatter, or BSP gameplay.
- No texture/material polish beyond biome flat colors + basic lighting.
- No infinite-2D variant yet (the graph layer is designed to allow it; not built now).
- No erosion simulation — plate uplift + rivers carving is the ceiling for now.

## Deferred asks — not yet scoped as milestones (2026-08-28)

Bruno asked where four specific things stand; capturing them here so they survive between sessions rather than living only in chat. None of these have a milestone yet — they're candidates for scoping after M5's integration decision, not in-flight work.

- **Real planet-scale rendering** — tracked: this *is* M4b (chunking, done 2026-08-28) + M4c (per-chunk LOD, done 2026-08-28). Chunking and 2-level LOD both landed; visibility culling (frustum/horizon) is the natural next addition, not yet done — see M4c's "still not implemented" note above.
- **Zooming into the surface for detail** — partially tracked: M4c gives LOD (done), M4d gives high-res colliders near the player (done). But actual close-up visual detail (surface noise/micro-relief on top of the flat per-cell shading) has no milestone — the Non-goals line above excludes it from this POC without saying when it comes back.
- **Rivers as more than lines** — three separate items, only one tracked:
  - Width-varying 3D rendering (using the flow-accumulation data that already exists) — tracked as a deferred item in the M2.5 bullet above (needs three.js's fat-line addon or a ribbon mesh).
  - Carving an actual channel into the terrain mesh geometry (not just an overlay line) — **not tracked anywhere**, would need scoping once chunk mesh-building (M4b) exists to carve into.
  - **New (2026-08-28)**: rivers currently read as too geometric/mechanical — straight corner-to-corner segments, no natural meander or bank detail. Likely needs the same "noisy edges" treatment M2.5 explicitly deferred for coastlines (jittering the path within a constrained corridor so it doesn't look mechanically straight), applied to river polylines too. **Not tracked anywhere** — new idea, no milestone.
- **In-game texturing, shoreline shading, biome blending** — shore foam/shallows has a real hook already (Layer 2's optional signed-distance-to-coast attribute), but texturing and blended (non-hard-edged) biome transitions are only named as *excluded from this POC* in Non-goals above, not captured as a future intent. **Not tracked as a real ask anywhere else** — needs its own scoped item once M5 decides on integration.
- **New (2026-08-31) — continent geometry, not river tracing, is what's really capping river length.** Surfaced while implementing the "River hydrology rework" entry below: at default tectonics settings (`plateCount: 10`, default `targetLandFraction`), a full corner-to-nearest-coast BFS showed max hop-distance from any land corner to the coast was only 5-15 even at 3000 cells, average under 2 — landmasses are geometrically narrow relative to the corner graph's resolution, so *no* river-tracing algorithm can produce a much longer river than that allows. Fixing this is a tectonics/land-generation lever (bigger single continents via fewer, larger plates; a higher `targetLandFraction`; or a broader continent-interior swell radius), not a `rivers.ts` one. **Not tracked as a milestone anywhere** — new finding, no scoping done yet.

## World profiles + per-cell terrain features (spike, 2026-08-29)

Bruno's ask, ahead of M5: a civilisation-game feel at large scale, where a single cell can *be*
a discrete landform (a whole mesa, volcano, crater) rather than only an emergent blend of many
cooperating cells' tectonic elevation; other world types (starting Moon-like); and — his own
addition — lava lakes and lava rivers "for some planets," including a planet-scale molten ocean
on a young/hot world, not just a puddle by a volcano. Explicitly scoped as a spike: prove the
direction, not a finished system.

- **`IWorldProfile`** (`worldgen/core/world-profile.ts`): named bundles (`terran`/`moon`/
  `volcanic`/`protoplanet`) of overrides across the *existing* tectonics/climate/biome param
  bags plus the new feature params and an `oceanSubstance: 'water' | 'lava'` flag — not a new
  pipeline. `terran` is an intentional no-op. `climate.ts` gained one real addition,
  `baseTemperatureOffset`, so `moon` reads as cold/airless, not just dry.
- **`computeFeatures()`** (`worldgen/core/features.ts`): per-cell `Feature` typing
  (volcano/mesa/crater/lava_lake) plus real shape stamps, reusing **`geological-features-lab`'s
  analytic height functions** (relocated verbatim, framework-free, into
  `worldgen/core/geological-shapes.ts` — cone+rim+crater+erosion, bowl+rim+ejecta,
  cap+talus+edge, far better than a from-scratch flatten/dig) instead of reinventing landform
  shapes. A feature's footprint is `cellsWithinHops()` (reused from `terrain-edits.ts`) around a
  seeded-candidate site cell; each footprint cell's own elevation is resampled through the
  matching geological function, projected onto a local tangent plane (the same gnomonic
  construction `buildColliderPatch()` uses), and the result materializes into a fresh per-cell
  array via `buildFeatureElevation()` — same "copy once, overwrite touched indices" shape as
  M4e's `buildEffectiveElevation()`, composed the same way (features first, player edits win on
  top). No changes needed to `chunking.ts`/`collider-patch.ts`/`sample-elevation.ts` at all: none
  of them call `sampleElevation()` internally, they just read whichever `elevation[]` array
  they're handed, which is exactly the property the M4e edit layer already depended on.
- **Lava is two different mechanisms, not one** — Bruno's correction mid-spike, since the first
  draft only modeled a small volcano-adjacent puddle: (1) `Feature: 'lava_lake'` tags an existing
  `waterBodyKind === 'lake'` cell near a volcano instance — local, small, coexists with an
  ordinary water ocean elsewhere. (2) `WorldProfile.oceanSubstance: 'lava'` (the `protoplanet`
  preset) substance-flags the *entire* largest `classifyWaterBodies()` component unconditionally
  — no volcano-adjacency check, and every `traceRivers()` path already drains into it by
  construction, so all rivers on that profile are lava rivers for free, no separate
  source-selection logic needed.
- **River rendering upgraded from a flat debug line to a flowing ribbon** — relocated
  `river-lab`'s POC (`river-lab/river-system.ts`, a *different* problem from
  `rivers.ts::traceRivers()`: that file assumes a path already exists and handles width/flow
  interpolation + the actual ribbon mesh/shader, `traceRivers()` decides where the path runs) into
  `worldgen/render/flow-path.ts`. Its flat-plane ribbon math doesn't carry over as-is to a sphere
  (the perpendicular/width construction assumed a fixed world-up axis), so `cell-planet-lab`
  builds its own spherical ribbon geometry per river path (`perpendicular = cross(radial,
  tangent)`) but reuses the relocated file's shader materials unchanged (`createWaterFlowMaterial()`
  / new `createLavaFlowMaterial()`) since those are UV-based and geometry-agnostic. Width comes
  from `sqrt(riverFlow)` — `rivers.ts`'s own doc comment already named this as the intended use of
  that field.
- **Crater scale**: only small/mid craters this spike (`sampleCrater()`'s flat-tangent-plane
  approximation holds up to a few percent of planet radius). Giant multi-ring impact basins
  (lunar-mare scale — real maria are later lava-flooded basins, a natural but separate tie-in to
  the lava work above) would need a true angular-distance formula or generation closer to
  tectonics' own multi-hop boundary spreading; not attempted here.
- **Lab wiring**: `/cell-planet-lab` gained a world-profile button group (mirroring the
  world-size-tier picker), a `'feature'` 2D/3D map mode, a per-feature-type count stat, and the
  ribbon river/lava rendering above.
- **Non-goals for this spike**: no mesh-level vertical cliff faces for mesas (corner-blend skirt
  only); no lava heat/damage gameplay; no new `Biome` values (features layer on top of, don't
  replace, climate biomes); no dedup of the new spherical ribbon builder against
  `flow-path.ts`'s flat one.
- Verification: `ng build triangular-engine --configuration development` and
  `ng build demo-app --configuration development` both clean. Bruno's own in-browser check
  (standing practice for this lab) is the remaining step — pick each profile and confirm visual
  distinctness, toggle `'feature'` map mode and confirm feature cells are visibly *shaped*
  differently (raised rims, flat mesa tops), not just recolored.

## River hydrology rework: flux accumulation & navigability (scoped + implemented 2026-08-31)

Ahead of M5, and following the 3 river-tracing bug fixes noted in M2.5/M3 above (rivers no
longer skirt lake/ocean edges, cross ice caps, or dead-end mid-nowhere): Bruno wants a real
gameplay distinction between **boat-navigable rivers** and **springs/creeks that aren't**, plus
generally longer/more-connected rivers than the current source-sampling model produces. Before
scoping a fix, compared our approach against **Azgaar's Fantasy Map Generator**
(`https://github.com/Azgaar/Fantasy-Map-Generator`, cloned read-only to
`D:\external\Fantasy-Map-Generator` for this comparison — not a dependency, not vendored) per
Bruno's explicit "don't reinvent the wheel" ask, with the constraint that any resulting design
stays single-source-of-truth in `worldgen/core` (both `cell-planet-map-page` and
`cell-planet-lab-page` already consume one `buildPlanetEcology()` output — see Architecture
above — and this must not fork per-renderer).

**What the comparison found** (full detail in that session's chat, condensed here):

- **Root cause of short/sparse rivers**: `rivers.ts::traceRivers()` seeds a fixed `sourceCount`
  of random qualifying corners and walks each downhill independently. Azgaar instead runs
  precipitation-driven flow accumulation across *every* land cell (a D8-style flow-accumulation
  pass) — a cell only becomes a river once its accumulated flux crosses a threshold. River
  network shape emerges from climate + terrain slope everywhere, not from how many random
  corners happened to qualify. This is the structural fix for "rivers are too short" and the
  main item scoped below.
- **Navigability**: Azgaar ships exactly the pattern already proposed for Bruno's ask before the
  clone happened — a single global constant (`MIN_NAVIGABLE_FLUX`) and `isNavigable(cellId)`
  checking accumulated flux against it. Directly validates building navigability as a flow
  threshold on our existing `riverFlow` field; folded into the scope below rather than shipped
  standalone, since it's far more meaningful once flux comes from real accumulation than from
  the current per-source walk (a "big" river today is just an accident of overlapping source
  paths, not a real drainage basin).
- **Explicit tributary/parent tracking**: Azgaar marks which of two converging flux paths is the
  tributary at a confluence (parent/basin ids). We currently only get merged-looking rivers
  because two independent source-walks happen to land on the identical deterministic downhill
  path afterward — no real relationship is recorded. Falls out for free once Phase 1 below tracks
  per-cell flux directly, so it's bundled in rather than deferred again.
- **Deliberately not carried forward**: Azgaar's full iterative global depression-filling
  (heavier than our targeted per-pit `findSpillExit`, which already fixed the dead-end bug
  adequately); their tapered-polygon ribbon rendering (`getRiverPath()`) — we already have
  spherical ribbon rendering from the World Profiles spike above (`flow-path.ts` /
  `createWaterFlowMaterial()`); their length-percentile name/type taxonomy (Creek/River/Brook/
  Fork) — cosmetic naming, not the gameplay distinction Bruno actually asked for.

**Scope (own milestone, sequenced, none of it started):**

**What actually shipped diverges from the scope above — the planned "Phase 1" (real D8-style flux
accumulation, catchment-threshold classification) was built, measured, and abandoned in the same
session, for a concrete, empirically-confirmed reason:**

- **This generator's terrain doesn't have valley structure for flow to converge into.** Tectonics
  elevation (see Layer 1 above) is shaped by plate-boundary uplift + continent-interior swell —
  broad domes and ridge lines, not the carved dendritic drainage networks real DEMs have (from
  actual geologic erosion). A downhill walk needs *convergence* (multiple points funneling into
  the same corner) to build a meaningfully-sized catchment; domes don't have that. Verified with a
  throwaway `tsx` script (not committed): even after 8 passes of neighbor-averaging smoothing
  before routing (to rule out the per-cell 0.08-amplitude texture-noise term in `elevation.ts`
  as the culprit), the largest observed catchment stayed under ~3% of a planet's land corners, at
  every tested cell count from 300 to 3000. Switching the accumulation graph from corners (always
  exactly 3 neighbors) to cells (5-7 neighbors, much friendlier to convergence in principle) made
  no measurable difference either — confirming the bottleneck is the elevation *field's shape*,
  not the graph's branching factor. Real D8 accumulation is a well-understood, correct technique;
  it just needs terrain with valleys to work on, which this generator doesn't produce.
- **A second, independent finding, since it was needed to explain why length barely improved
  either way: candidate river sources were geometrically capped at ~2 hops from the coast,
  regardless of algorithm.** The pre-existing `minSourceMoisture: 0.4` gate (unchanged from the
  original model, kept in the final design below) requires a source corner's moisture — a BFS
  distance-from-ocean field with a *fixed* `moistureFalloffRadius: 4` graph-hop falloff (see
  `climate.ts`) — to be >= 0.4, which algebraically caps a qualifying corner at ~2 hops from
  water, at any cell count. On top of that, a full corner-to-nearest-coast BFS (also a throwaway
  script) showed the actual generated continents at default settings (`plateCount: 10`,
  `targetLandFraction` default) are themselves geometrically narrow: max hop-distance from any
  land corner to the nearest coast was only 5-15 even at 3000 cells, average under 2. **No source
  selection or accumulation model can produce a longer river than the landmass's own geometric
  depth allows** — this is a continent-generation/tectonics-parameter property, not a
  `rivers.ts` one, and is now the real, correctly-identified next lever for "rivers feel short" if
  Bruno wants to pursue it further (see the new Deferred-asks bullet below) — not the moisture
  gate, which only mattered a little once actually measured (avg river length 2.5→2.8 corners at
  300 cells with the gate removed entirely; not the dominant factor either).

**What shipped instead — same file, same public API shape (`traceRivers()`/`IPlanetRivers`),
pragmatic given the above:**

- Headwaters are chosen deterministically (candidates ranked by elevation, tallest first) instead
  of randomly sampled, up from the old fixed `sourceCount: 12` default to `40` — a real,
  measurable improvement over random sampling (a random pick was just as likely to land barely
  above the elevation gate as near the top of it), even though it can't beat the geometric cap
  above.
- **Explicit confluence/tributary tracking survived the pivot intact** — `computeDownhillStep()`
  (the per-corner stepping logic: water-cell awareness, frozen termination, pit spill-over, all
  unchanged from before this rework) is now computed once and shared, and each headwater's walk
  stops the moment it reaches a corner an earlier (taller-sourced) river already claimed, recording
  that river as its parent (`riverParent`/`riverBasin`, new fields) instead of re-tracing the
  shared trunk. Real confluences are rare on this terrain (same root cause as above) but do occur
  and are now recorded properly when they do, including a found-and-fixed self-loop edge case
  (a chained pit-spill occasionally routed a headwater's walk back into its own already-claimed
  path, previously recorded a corner as its own parent — fixed by treating a walk revisiting its
  own claim as a plain dead end instead of a merge).
- **`riverFlow` is now confluence-count x a within-river length-progression term**, not a bare
  visit count. Since real confluence is rare here, a river widening only at rare merges would
  mostly look like a flat line with an occasional jump; the length term (1x at each river's own
  source, `1 + lengthGrowthFactor`x at its own mouth, normalized to *that river's own length* so
  a short spring and a long trunk both read as "widening downstream" at a comparable rate) stands
  in for the countless small unmodeled tributaries/groundwater inflow a real river also widens
  from. Confluence contributions propagate downstream of the merge point too (a subtree-size
  aggregation over the parent/basin tree), not just at the exact corner they occur.
- **`minNavigableFlow`** (mirrors Fantasy Map Generator's `MIN_NAVIGABLE_FLUX`/`isNavigable()`):
  resolved to a concrete number (`minNavigableFlowFraction` x the largest flow actually observed
  among a planet's river points, not diluted by all land corners) and exposed on `IPlanetRivers`
  rather than a boolean array, so both `cell-planet-map-page` and `cell-planet-lab-page` compare
  the exact same threshold against the exact same `riverFlow` field — including after either
  resamples it for fractal detail (`ecology.ts`'s `addFractalDetailWithFlow()`), which a
  precomputed boolean array would have had to be threaded through in lockstep to stay correct.
  `cell-planet-map-page`'s river overlay now strokes navigable segments in a deeper, more
  saturated blue than spring/creek segments — the concrete visual answer to Bruno's original
  boat-vs-spring ask. `cell-planet-lab-page`'s 3D ribbon and 2D overlay width formulas were left
  untouched (already safe against the new flow units — no NaN risk, no hardcoded assumption that
  broke), but don't yet carry the navigable/spring color split; flagged, not done this pass.
- Deliberately not carried forward from Fantasy Map Generator (unchanged from the original scoping
  above): full iterative global depression-filling, tapered-polygon ribbon rendering (already have
  spherical ribbons from the World Profiles spike), length-percentile name/type taxonomy.
- Verification: `rivers.spec.ts` rewritten for the new API/semantics (flow monotonicity, basin/
  parent consistency, a real-confluence-exercising test at a calibrated seed/cell-count, a
  navigability-threshold-splits-the-range test) — 136/138 `test:triangular-engine:worldgen` passing
  (the 2 failures are `sample-elevation.spec.ts`, pre-existing/unrelated, confirmed via `git
  status` showing that file untouched). `ng build triangular-engine` and `ng build demo-app`
  (`--configuration development`) both clean. Bruno's own in-browser check is the remaining step —
  confirm the map's navigable/spring color split reads correctly and rivers still look reasonable
  end-to-end, per this workspace's standing "visual acceptance is a user check" practice.

## Mountain ridge network pass (implemented 2026-09-12; refined 2026-09-13)

The existing `ridgeCellIds` correctly marked cells touching continent-continent collision
boundaries, but there was no explicit line network to render between those mountain cells. Added a
compact ridge pass in `worldgen/core/ridges.ts`:

- **Tectonic spines**: convergent continental boundary adjacencies are stitched through the centres
  of their mountain cells. Endpoints and junctions split the network, so a chain, branch/star, or
  parallel boundary produces separate readable paths. This deliberately occupies cell-to-cell
  links, leaving shared Voronoi edges for rivers and coastlines.
- **Terrain skeletons**: connected high land cells outside the tectonic paths are reduced to a
  deterministic maximum-spanning forest. This preserves narrow mountain chains and branches
  without drawing every edge of a broad alpine patch.
- **Summits**: locally highest land cells above a normalized relief threshold become `ridgePeaks`;
  isolated mountain cells therefore remain points instead of receiving an invented line.
- **Public ecology data**: `IPlanetEcology` now exposes `ridgePaths`, `ridgePathStrength`,
  `ridgePeaks`, and `ridgePeakCellIds`. The core remains deterministic and unit-sphere based.
- **Rendering**: `/cell-planet-map` has a `Mountain ridges` toggle and draws brown ridge paths plus
  summit dots. `<planetView>` also accepts `[showRidges]` and renders the same ecology paths on the
  globe. The public worldgen README and changelog were updated.
- **Shared visual detail**: each cell-to-cell link now receives a deterministic local Voronoi-style
  corridor route. Jittered candidate sites are sampled between the two exact cell-centre anchors,
  then a short lowest-cost route is selected. The resulting unit-sphere points are shared by the
  2D and 3D overlays, so added bends cannot make the map and globe disagree. `IRidgeParams.ridgeDetail`
  exposes station count, lane count, corridor width, and site jitter for tuning.
- **Terrain and river awareness**: ridge extraction now scores each candidate link using cell height,
  lateral crest relief, and convergent plate strength. It builds one maximum-spanning mountain
  skeleton from that combined score, which avoids treating every high-cell connection as equal and
  reduces the arbitrary zigzags seen in the earlier boundary-first network. Ecology traces rivers
  first and passes their paths into ridge extraction; a candidate ridge link that touches a river
  corridor is removed, allowing the river to cut a visible gap in the ridge. `riverClearance` on
  `IRidgeParams` controls the local corridor margin.
- **Verification**: added `ridges.spec.ts`; the library and demo builds pass, and a direct generated
  planet smoke check confirmed non-empty paths/peaks, aligned strength arrays, and unit-sphere
  points. The detail smoke check also confirmed paths keep cell-centre anchors and gain inserted
  unit points. The Karma worldgen bundle built, but ChromeHeadless could not start in the local
  environment because of its GPU/process setup.

This pass establishes ridge topology, shared detail geometry, elevation-aware selection, and river
compatibility. It does not yet carve channels into the terrain, alter river routing from ridge
geometry, or add separate parallel crest branches; those remain the next visual/geological
refinements once the layouts have been reviewed in the map.

## Terrain variety fixes: island/lake dotting, arid-belt calibration, ice-cap variability (2026-08-31)

**TL;DR:** Bruno flagged three symptoms after the rivers rework: (1) high-cell-count continents get
speckled with single-cell islands/lakes, (2) almost every generation is desert/savanna-dominated
regardless of seed, (3) the ice cap is always exactly the same size. All three traced to real,
independent root causes in `elevation.ts`/`climate.ts`/`biomes.ts` — fixed below, all defaults,
no new params required from callers, `test:triangular-engine:worldgen` and both builds clean.

- **Island/lake dotting** — `computeElevation()`'s per-cell noise (`noiseAmplitude`) was applied
  fully independently per cell (pure white noise, zero spatial correlation), and the sea-level cut
  is a global elevation percentile (`targetLandFraction`) that has no relationship to how much area
  continental-type plates actually cover (plate type is assigned per-plate by *count* via
  `oceanicFraction`, not by area, and plate sizes are an unbalanced random flood-fill — see
  `plate-tectonics.ts`). Whenever continental area doesn't land near exactly `targetLandFraction`
  (the common case), the percentile cutoff falls *inside* one base elevation's noise band
  (`continentalBase ± noiseAmplitude` or `oceanicBase ± noiseAmplitude`), and since the noise has no
  spatial coherence, every cell whose roll crosses that line becomes an isolated single-cell speck —
  more of them, and more visible, at higher cell counts (more independent rolls). Fixed two ways:
  the noise is now run through `smoothField()` (a couple of neighbor-averaging passes,
  `noiseSmoothingPasses`) before being added, so it forms coherent patches instead of white noise;
  and `removeSmallRegions()` flood-fills connected land/water components after the sea-level cut and
  merges anything under `minRegionCellFraction` of total cells into whatever surrounds it — a
  backstop that guarantees no stray specks survive even from other sources (e.g. a boundary
  contribution landing right on the cutoff). `elevation.spec.ts` gained a test asserting no
  component below the configured minimum survives, checked at 1500 cells across three seeds.
- **Desert/savanna dominance, same every generation** — `aridBeltCenter`/`aridBeltWidth`
  (`climate.ts`) are fixed global constants, not seeded, and the old values (0.22/0.28) put the
  belt's dry influence over almost the *entire* tropical band (`hotTemperatureThreshold`'s `isHot`
  cutoff is ~`|y|<=0.325`) with essentially no latitude left wet enough to ever read as rainforest —
  even the literal equator got a ~21% moisture cut. Since neither the belt's position nor the
  tropical band's position varies by seed, this happened identically in every generation regardless
  of where continents landed. Real subtropical deserts sit poleward of the tropics, not stacked on
  top of them. Recalibrated to 0.32/0.22 — the belt's zero-influence edge now sits at `|y|=0.10`,
  leaving a genuinely wet equatorial ring where rainforest can form, and peak dryness centers at
  `|y|=0.32` (~19°), right at the hot/not-hot boundary, giving a realistic equator(wet) → tropical
  desert → subtropical steppe progression instead of one uniform arid band. Verified via a throwaway
  script across 6 seeds at 300 cells: desert dropped from dominant to a rare, targeted biome (0-2
  cells per seed) while rainforest/jungle/steppe all became consistently present — the actual
  variety goal, not just smaller numbers. `biomes.spec.ts`'s existing "deserts can be coastal" test
  was checking a single seed for a now-rare biome and needed updating to check across several seeds
  (matches this file's existing "at any seed" test pattern) rather than depending on one seed
  happening to roll a coastal desert.
- **Ice cap always identical** — `computeClimate()`'s temperature is a pure function of latitude
  (`1 - 2·|y|`) plus a small elevation lapse, with zero seed/noise term at all, so
  `coldTemperatureThreshold` always carved out exactly `|y| >= 0.675` as ice cap, every single
  generation. Fixed in `biomes.ts` (not climate.ts — freezing is a biome-classification decision,
  not a climate one): `IBiomeParams` gained `seed`/`iceCapVariability` (default `0.25`); when a seed
  is given, one random scalar is drawn *once per planet* (not per-cell — a per-cell jitter would
  make the cap boundary jagged instead of just a different size) and subtracted from
  `coldTemperatureThreshold`, so the effective threshold is always at or colder than the configured
  default — `coldTemperatureThreshold` is the *largest* extent any generation can produce, matching
  Bruno's ask that "the current level should be the max." `ecology.ts` now always passes a
  decorrelated seed (`tectonics.seed + 2`) into `computeBiomes()`, so the actual game always gets
  variability; direct unit tests calling `computeBiomes()` without a seed keep today's exact
  deterministic behavior unchanged. `biomes.spec.ts` gained a test asserting seeded runs are never
  larger than the unseeded baseline and that at least one seeded run is smaller.
- Known minor gap, not fixed this pass: `rivers.ts`'s own `frozenTemperatureThreshold` (routes
  rivers around ice) doesn't share the same per-generation draw as `biomes.ts`'s effective cold
  threshold, so on a generation with a shrunk ice cap a river could still terminate slightly inside
  what's now tundra/taiga rather than literal ice — a small single-source-of-truth gap, not
  incorrect-looking terrain (tundra ground is still cold). Not in scope for this pass; flag if it
  becomes visible in practice.
- Verification: `test:triangular-engine:worldgen` — 138/140 passing (2 new tests added this pass,
  both passing; the 2 failures are `sample-elevation.spec.ts` and
  `planet-view.component.spec.ts`, both pre-existing/unrelated, confirmed via `git status` showing
  neither file touched this session). `ng build triangular-engine` and `ng build demo-app` both
  clean. Bruno's own in-browser check is the remaining step, per this workspace's standing "visual
  acceptance is a user check" practice.

## References

- Runbook [030_cell_planet_25d_map.md](030_cell_planet_25d_map.md) — planned tilted planar
  map using the shared cell world and reusable GPU-morph clipmap; tracks integration separately.
- Amit Patel, *Polygonal Map Generation for Games* (Red Blob Games) — cell-graph elevation, moisture, biomes, rivers; V4 is this on a sphere with plates added.
- Runbook [019_cdlod_celestial_migration.md](019_cdlod_celestial_migration.md) — the CDLOD pipeline V4 deliberately does not assume.
- Runbook [004_multi_surface_terrain.md](004_multi_surface_terrain.md) — shape-agnostic surface domains (relevant for the later flat/infinite variant).
- Runbook [024_sublibrary_reusability_boundaries.md](024_sublibrary_reusability_boundaries.md) — the dimensionless-core / radius-at-boundary rule M4d's scoping notes above follow, plus the existing `celestial`/`HOME_PLANET` coupling flagged as a wart not to repeat.
